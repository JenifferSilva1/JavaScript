var contadora=0;
while (contadora<10) {
    document.write(contadora+"</br>");
    contadora=contadora+1;
}

// br pula linha aqui também
// document.write mostra em linha

var contadora=0;
while (contadora<10) {
    console.log(contadora);
    contadora=contadora+1;
}
// F12 no navegador para ver

var contadora=0;
while (contadora<10) {
    window.alert(contadora);
    contadora=contadora+1;
}
// precisa ir clicando no ok da caixa de texto

// inner.html também exibe - mas tem que trocar - pela id